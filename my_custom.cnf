[galera]
#bin-log-config
server-id=1
log_bin = /bitnami/mariadb/bin-log
log_bin_index = /bitnami/mariadb/bin-log.index
binlog_format=row
expire_logs_days=10
max_binlog_size=100M
binlog_do_db=network_alliance